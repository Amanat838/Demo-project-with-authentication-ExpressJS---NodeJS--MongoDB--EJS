<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        
        <h5 class="mb-5">You can create a new post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind" class="p-3 block resize-none bg-transparent border-2 w-1/3 border-zinc-700" name="content" > <%= post.content %> </textarea>
            <input class="px-3 py-2 bg-yellow-500 block mt-3 rounded-md w-40" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>